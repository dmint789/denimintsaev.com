export enum ListType {
  Results = 'results',
  KanjiList = 'kanjiList',
}

export enum ViewType {
  Default = 'default',
  KanjiOnly = 'kanjionly',
}

export enum Mode {
  Replace = 'replace',
  Add = 'add',
}
